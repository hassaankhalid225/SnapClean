import 'package:flutter/material.dart';

class AppColors {
  // Dark Theme
  static const darkBackground    = Color(0xFF0F0F0F);   // Near black
  static const darkSurface       = Color(0xFF1A1A1A);   // Card background
  static const darkSurfaceAlt    = Color(0xFF222222);   // Slightly lighter
  static const darkBorder        = Color(0xFF2E2E2E);
  
  // Accent — warm amber/golden
  static const primary           = Color(0xFFE8A838);   // Golden amber
  static const primaryLight      = Color(0xFFF5C76A);
  static const primaryDark       = Color(0xFFB8821A);
  
  // Status colors
  static const keepGreen         = Color(0xFF4CAF82);
  static const deleteRed         = Color(0xFFE05C5C);
  static const pendingAmber      = Color(0xFFE8A838);
  
  // Text
  static const textPrimary       = Color(0xFFF2F2F2);
  static const textSecondary     = Color(0xFF9A9A9A);
  static const textHint          = Color(0xFF5A5A5A);
  
  // Light Theme equivalents also define
  static const lightBackground   = Color(0xFFF8F6F2);   // Warm white
  static const lightSurface      = Color(0xFFFFFFFF);
  static const lightSurfaceAlt   = Color(0xFFF0EDE6);
  static const lightBorder       = Color(0xFFE2DDD6);
  static const lightTextPrimary  = Color(0xFF1A1A1A);
  static const lightTextSecondary= Color(0xFF6B6B6B);
}
